// ALLXAI beta config.
// Fill these in from Supabase: Project Settings > API.
// The anon key is safe to ship in frontend code; access is enforced by the
// row level security policies in supabase/schema.sql.
// Leave both empty to run in local preview mode (data stays in this browser only).
window.ALLXAI_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
